# setup.py
from setuptools import setup, find_packages

setup(
    name="project1",          # you can pick any name
    version="0.1",
    packages=find_packages(),  # finds your processor/ folder
)
